<%= form_with url: msoa_checks_path do |f| %>
  <%= f.text_field :postcode, placeholder: t('msoa_checks.form.placeholder'), value: params[:postcode] %>
  <%= f.submit t('msoa_checks.form.submit') %>
<% end %>
